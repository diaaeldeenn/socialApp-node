export enum CommentAvalability {
  allow = "allow",
  disAllow = "disAllow",
}

export enum PostPrivacy {
  public = "public",
  private = "private",
  onlyMe = "onlyMe",
}

export enum ON_Model {
  Post = "Post",
  Comment = "Comment",
}
